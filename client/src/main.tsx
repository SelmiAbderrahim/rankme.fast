// Deprecated entrypoint. The app now boots via `entry-client.tsx`, which
// index.html loads directly (it decides hydrate vs client-render based on
// whether the page was server-rendered). Kept as an empty stub.
export {};
