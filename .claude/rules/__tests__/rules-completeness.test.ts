// Legacy MERN-starter test — the rules it referenced were never ported into
// rankme.fast. Kept as an empty guard file so an orphaned import path in
// external tooling does not fail. The authoritative rules-consistency check
// runs in server/src/shared/testing/docs-consistency.test.ts.
export {};
